## `ml/` (local tooling)

This folder is intentionally **not** the training codebase.

- **Runs locally**: preprocessing, artifact validation, lightweight CPU inference checks
- **Runs on Kaggle**: training notebooks + model definitions + ONNX export

